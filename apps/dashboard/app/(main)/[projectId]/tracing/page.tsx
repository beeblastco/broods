"use client";

/** Tracing page for monitoring agent execution traces. */
export default function TracingPage() {
    return (
        <div className="flex h-full flex-col items-center justify-center gap-2">
            <h1 className="text-lg font-semibold text-white">Tracing</h1>
            <p className="text-sm text-white/50">Agent execution traces will appear here.</p>
        </div>
    );
}
