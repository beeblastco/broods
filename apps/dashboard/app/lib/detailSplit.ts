/**
 * Pixel sizes of the table-plus-detail split. Kept out of the component so the
 * Playwright spec can assert the same numbers without importing JSX.
 */
export const DETAIL_DEFAULT_WIDTH = 360;
export const DETAIL_MIN_WIDTH = 280;
export const TABLE_MIN_WIDTH = 320;
