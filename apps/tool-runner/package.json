{
  "name": "@broods/tool-runner",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun run scripts/build.ts",
    "check": "bunx tsc --noEmit",
    "test": "bun run build && bun test"
  },
  "devDependencies": {
    "@types/aws-lambda": "8.10.156"
  }
}
