name: Build Dashboard Image

on:
  push:
    branches:
      - main
      - dev
    paths:
      - 'apps/dashboard/**'
      - 'packages/convex/**'
      - 'package.json'
      - 'bun.lock'
      - '.github/workflows/build-dashboard.yaml'
  workflow_dispatch:

env:
  IMAGE_NAME: ghcr.io/beeblastco/filthy-panty-dashboard

jobs:
  build:
    runs-on: ubuntu-24.04
    # main builds against the production Convex deployment, everything else
    # against dev. Environment-scoped secrets/vars (CONVEX_DEPLOY_KEY,
    # NEXT_PUBLIC_CONVEX_URL, ...) override the repo-level dev defaults.
    environment: ${{ github.ref_name == 'main' && 'production' || 'development' }}
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup Bun
        uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Type-check dashboard
        run: cd apps/dashboard && npx tsc --noEmit

      - name: Validate build variables
        shell: bash
        env:
          NEXT_PUBLIC_CONVEX_URL: ${{ vars.NEXT_PUBLIC_CONVEX_URL }}
          CONVEX_DEPLOY_KEY: ${{ secrets.CONVEX_DEPLOY_KEY }}
        run: |
          if [[ -z "$NEXT_PUBLIC_CONVEX_URL" ]]; then
            echo "Missing repository variable: NEXT_PUBLIC_CONVEX_URL" >&2
            exit 1
          fi
          if [[ -z "$CONVEX_DEPLOY_KEY" ]]; then
            echo "Missing repository secret: CONVEX_DEPLOY_KEY" >&2
            exit 1
          fi
          # Convex deploy keys encode their target deployment. Refuse to ship a
          # main build that would deploy functions to a non-production deployment.
          if [[ "${{ github.ref_name }}" == "main" && "$CONVEX_DEPLOY_KEY" != prod:* ]]; then
            echo "main requires a production Convex deploy key (prod:...) in the 'production' environment secret CONVEX_DEPLOY_KEY" >&2
            exit 1
          fi

      - name: Build and push image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: apps/dashboard/Dockerfile
          push: true
          tags: |
            ${{ env.IMAGE_NAME }}:${{ github.ref_name }}
            ${{ env.IMAGE_NAME }}:${{ github.sha }}
          secrets: |
            CONVEX_DEPLOY_KEY=${{ secrets.CONVEX_DEPLOY_KEY }}
          build-args: |
            NEXT_PUBLIC_AGENT_GATEWAY_URL=${{ vars.NEXT_PUBLIC_AGENT_GATEWAY_URL }}
            NEXT_PUBLIC_CONVEX_URL=${{ vars.NEXT_PUBLIC_CONVEX_URL }}
            NEXT_PUBLIC_WORKOS_REDIRECT_URI=${{ vars.NEXT_PUBLIC_WORKOS_REDIRECT_URI }}
            NEXT_PUBLIC_FILTHY_PANTY_BASE_URL=${{ vars.NEXT_PUBLIC_FILTHY_PANTY_BASE_URL || 'https://app.beeblast.co' }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
