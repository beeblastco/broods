diff --git a/dist/index.js b/dist/index.js
index 99eca92075fed634678059855b7c8990fcdf0cc6..1a8359def91e12fcc92021a150d07d41e86eeb1b 100644
--- a/dist/index.js
+++ b/dist/index.js
@@ -228,10 +228,11 @@ function createCodex(settings = {}) {
       });
       if (coords) {
         try {
-          const attachUrl = await sandboxSession.getPortUrl({
+          const attachBaseUrl = await sandboxSession.getPortUrl({
             port: coords.port,
             protocol: "ws"
-          }) + `?agent_bridge_token=${encodeURIComponent(coords.token)}`;
+          });
+          const attachUrl = `${attachBaseUrl}${attachBaseUrl.includes("?") ? "&" : "?"}agent_bridge_token=${encodeURIComponent(coords.token)}`;
           const attachChannel = new SandboxChannel({
             connect: () => openWebSocket(attachUrl),
             outboundSchema: outboundMessageSchema,
@@ -339,10 +340,11 @@ function createCodex(settings = {}) {
         })
       });
       void drainBridgeProcessStream(proc.stdout);
-      const wsUrl = await sandboxSession.getPortUrl({
+      const bridgeBaseUrl = await sandboxSession.getPortUrl({
         port: boundPort,
         protocol: "ws"
-      }) + `?agent_bridge_token=${encodeURIComponent(token)}`;
+      });
+      const wsUrl = `${bridgeBaseUrl}${bridgeBaseUrl.includes("?") ? "&" : "?"}agent_bridge_token=${encodeURIComponent(token)}`;
       const channel = new SandboxChannel({
         connect: () => openWebSocket(wsUrl),
         outboundSchema: outboundMessageSchema,
diff --git a/src/codex-harness.ts b/src/codex-harness.ts
index ae51a7b3106de24ae72a4cfb5e6bbaf5c0ce68fe..56b5bdf5aa746ac5ad0792907120f846cba624aa 100644
--- a/src/codex-harness.ts
+++ b/src/codex-harness.ts
@@ -272,11 +272,11 @@ export function createCodex(
        */
       if (coords) {
         try {
-          const attachUrl =
-            (await sandboxSession.getPortUrl({
-              port: coords.port,
-              protocol: 'ws',
-            })) + `?agent_bridge_token=${encodeURIComponent(coords.token)}`;
+          const attachBaseUrl = await sandboxSession.getPortUrl({
+            port: coords.port,
+            protocol: 'ws',
+          });
+          const attachUrl = `${attachBaseUrl}${attachBaseUrl.includes('?') ? '&' : '?'}agent_bridge_token=${encodeURIComponent(coords.token)}`;
           const attachChannel: CodexChannel = new SandboxChannel({
             connect: () => openWebSocket(attachUrl),
             outboundSchema: outboundMessageSchema,
@@ -411,11 +411,11 @@ export function createCodex(
       });
       void drainBridgeProcessStream(proc.stdout);
 
-      const wsUrl =
-        (await sandboxSession.getPortUrl({
-          port: boundPort,
-          protocol: 'ws',
-        })) + `?agent_bridge_token=${encodeURIComponent(token)}`;
+      const bridgeBaseUrl = await sandboxSession.getPortUrl({
+        port: boundPort,
+        protocol: 'ws',
+      });
+      const wsUrl = `${bridgeBaseUrl}${bridgeBaseUrl.includes('?') ? '&' : '?'}agent_bridge_token=${encodeURIComponent(token)}`;
 
       const channel: CodexChannel = new SandboxChannel({
         connect: () => openWebSocket(wsUrl),
