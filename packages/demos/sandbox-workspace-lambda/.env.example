# Env for the 'sandbox-workspace-lambda' demo.
# Deployed core service URLs (from sst deploy outputs) — required by every demo.
ACCOUNT_SERVICE_URL=
AGENT_SERVICE_URL=

# Demo-specific configuration:
SANDBOX_SMOKE_VAR=
